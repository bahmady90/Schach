export type svgProps = {
    color: string,
    frameColor: string
} 

export type figureType = {
    pieceCode: string,
    square: string
}

export type boardElementType = {
    type: string,
    color: string
} | null

